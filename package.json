{
  "name": "paper2test-admin",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch & npx serve dist -l 5173",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": { "preact": "^10.24.0" },
  "devDependencies": { "esbuild": "^0.25.0", "typescript": "^5.6.0", "serve": "^14.2.0" }
}
